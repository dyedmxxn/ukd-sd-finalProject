# Django final project
### Installation
In terminal: 
1) Clone repo: `git clone https://github.com/dyedmxxn/ukd-sd-finalProject.git`
2) Install dependencies: `uv sync`
3) Navigate to app directory: `cd app`
4) Run the local server: `uv run python manage.py runserver`
